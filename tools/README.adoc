= Werkzeuge
:toc: left
:toc-title: Inhalt
:toclevels: 3
:icons: font
:source-highlighter: rouge
:experimental:

ifdef::env-github[]
:tip-caption: :bulb:
:note-caption: :information_source:
:important-caption: :heavy_exclamation_mark:
:caution-caption: :fire:
:warning-caption: :warning:
endif::[]

Prüfung und Generatoren rund um `curriculum.yaml`. Sprache ist Python; einzige
Laufzeitabhängigkeit ist PyYAML. (Das Repository `klassen-setup` ist davon
unberührt und bleibt Bash.)

== Einrichtung

[source,bash]
----
python3 -m venv .venv
source .venv/bin/activate
pip install -r tools/requirements.txt        # Laufzeit
pip install -r tools/requirements-dev.txt    # zusätzlich für die Tests
----

== Aufrufe

Alle Werkzeuge werden aus dem Repository-Wurzelverzeichnis aufgerufen und
finden `curriculum.yaml` selbst.

[cols="2,3",options="header"]
|===
|Aufruf |Wirkung

|`python tools/check-curriculum.py`
|Führt alle Prüfungen aus, meldet Datei, Zeile, Ist- und Sollwert.
 Exit-Code 1, sobald eine Prüfung fehlschlägt.

|`python tools/check-curriculum.py --only budget,graph`
|Führt nur die genannten Prüfungen aus (Namen siehe `--list`).

|`python tools/check-curriculum.py --list`
|Listet die Prüfungen mit ihrer Nummer aus dem Design.

|`python -m pytest tools/tests`
|Fixture-Testsuite: je ein roter und ein grüner Fall pro Prüfung.
|===

== Konventionen

`tools/curriculum.py` ist das gemeinsame Lademodul. Es liest `curriculum.yaml`,
leitet die Modulpfade aus der Themen-ID ab und meldet fehlende Pflichtfelder mit
Zeilenangabe.

[source,python]
----
import tools.curriculum as c

model = c.load()
model.topics[0].index_path        # modules/<id>/index.adoc
model.theory_ue_by_block()        # Summen je Block
----

Jedes Werkzeug gibt Befunde im selben Format aus:

----
curriculum.yaml:212: budget: block 'governance' hat 10 UE Theorie, erlaubt sind 9
----
